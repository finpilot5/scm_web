# SCM 자동 일정 관리 시스템 기획서

## 1. 프로젝트 개요
### 1.1 목표
재고, 발주, 생산, 리드타임, 소비기한 데이터를 통합해 **52주 기준 생산·발주 계획**을 자동 생성하고, SCM 담당자의 To-Do를 캘린더로 제공하는 웹 시스템 구축.

### 1.2 해결하려는 문제
- 재고 부족/과잉 재고
- 발주 시점 판단 오류
- 생산 일정 충돌
- 소비기한 관리 실패
- 수작업 엑셀 운영

### 1.3 해결 방식
데이터 기반 자동 계산으로 아래를 생성:
- 생산 요청 시점
- 발주 시점
- 납품 일정

---

## 2. 핵심 기능 요구사항
### 2.1 통합 SCM 대시보드
한 화면에서 핵심 지표를 SKU별 카드 중심으로 제공.

| 항목 | 설명 |
| --- | --- |
| 현재 재고 | 창고 기준 실시간 재고 |
| 예상 사용량 | 판매 예측 기반 예상 소비 |
| 실제 사용량 | 실제 판매/출고 데이터 |
| 예상 발주량 | 자동 계산 발주 제안 |
| 실제 발주량 | 확정된 발주 데이터 |
| 생산 요청량 | 제품 생산 필요 수량 |
| 리드타임 | 생산/물류/원재료 리드타임 |
| 소비기한 | 유통기한 및 임박 상태 |

화면 특징:
- SKU 카드 UI
- 재고 위험도 표시(부족/과잉)
- 소비기한 임박 강조

### 2.2 52주 SCM 계획 자동 생성
계산식(기본):
`필요 생산량 = 예상 사용량 + 안전재고 - 현재재고`

시점 계산:
- 생산 시작일 = 납품일 - 생산 리드타임 - 물류 리드타임
- 발주일 = 생산 시작일 - 원재료 리드타임

결과:
- 52주 주차별 생산/발주/재고 계획 자동 생성

### 2.3 캘린더 기반 To-Do 자동 생성
업무 이벤트를 자동 생성:
- 원재료 발주
- 생산 시작
- 생산 완료
- 물류 출고

보기 모드:
- 주간 / 월간 / 연간

### 2.4 재고 시뮬레이션
사용자 조정 항목:
- 예상 판매량
- 재고
- 생산량
- 발주량

조정 시 52주 계획 즉시 재계산.

### 2.5 소비기한 관리
- 소비기한 임박 알림
- 폐기 예정 표시
- FIFO(선입선출) 우선 출고 지원

### 2.6 월간 수불(입고/출고) 원장 연동
첨부 엑셀(`5월 수불.xlsx`)처럼 월별 입고/출고 데이터를 원장 형태로 업로드하면, DB의 재고를 로트/소비기한 단위로 “누적 반영”한다.

- 업로드 범위
  - 입고(Receipt) / 출고(Issue) 수량 및 단위
  - 로트 번호(Lot No) / 소비기한(Expiry)
  - 거점/채널 코드(예: FC, B2B, F&B 등) 또는 창고 매핑키
  - (엑셀 포맷에 존재하는) O/X 같은 플래그 컬럼은 “거래 존재/부재” 여부로 해석
- 반영 로직
  - 재고(기초재고 + 입고 - 출고) = 기말재고가 맞는지 reconciliation 체크
  - 출고가 발생한 경우, **수불 원장(출고 OUT) 행을 입력 소스로** 로트/소비기한 기준 FIFO로 “실제 사용량(실적 수요)”을 산출
  - 산출된 실제 사용량은 52주 엔진의 수요/실사용량 지표에 반영(예측과 분리 표기)

### 2.7 재고 위험 알림
| 유형 | 설명 |
| --- | --- |
| 재고 부족 | 생산/발주 필요 |
| 과잉 재고 | 판매 촉진/계획 조정 필요 |
| 소비기한 임박 | 폐기 위험 |
| 발주 지연 | 생산 차질 위험 |

---

## 3. 데이터 모델(논리)
### 3.1 제품
| 필드 | 설명 |
| --- | --- |
| product_id | 제품 ID |
| product_name | 제품명 |
| category | 카테고리 |
| shelf_life | 소비기한(일) |
| safety_stock | 안전재고 |
| production_capa | 생산 CAPA(일/주 단위) |

### 3.2 재고
| 필드 | 설명 |
| --- | --- |
| product_id | 제품 ID |
| warehouse_id | 창고 ID |
| quantity | 수량 |
| lot_no | 로트 번호 |
| expiry_date | 소비기한 |

### 3.2.1 재고 수불(입고/출고) 트랜잭션
| 필드 | 설명 |
| --- | --- |
| transaction_id | 원장 고유 ID(엑셀 row 기준) |
| as_of_date | 기준일(엑셀 월/일자) |
| product_id | 품목 ID |
| warehouse_or_hub | 창고/거점 코드(엑셀의 FC/B2B/F&B 등을 warehouse_id로 매핑) |
| transaction_type | IN(입고) / OUT(출고) |
| qty | 수량 |
| unit | EA/kg 등 단위 |
| lot_no | 로트 번호(입고/출고 단위로 분리) |
| expiry_date | 소비기한(입고 로트에 필수, 출고 시에도 참조 가능) |
| source_ref | 엑셀의 BL/문서번호 등 원문 참조값 |
| fifo_consumption | 출고 시 FIFO로 소진된 로트별 사용량(내부 계산 결과) |

### 3.3 생산 계획
| 필드 | 설명 |
| --- | --- |
| product_id | 제품 ID |
| week_no | 주차 |
| production_qty | 생산량 |
| production_start | 생산 시작일 |
| completion_date | 생산 완료일 |

### 3.4 발주
| 필드 | 설명 |
| --- | --- |
| material_id | 원재료 ID |
| week_no | 주차 |
| order_qty | 발주량 |
| moq | 최소 발주량 |
| order_date | 발주일 |
| arrival_date | 입고일 |

---

## 4. 자동 계산 엔진 정의
### 입력
- 기초재고(초기재고)
- 월간 수불 원장(입고/출고) : 수불 기반으로 현재 재고를 누적하고, 출고 OUT을 입력 소스로 FIFO 실사용량 생성
- 예상 판매량(수요예측) : 계획(예측) 수요 생성
- 안전재고
- 생산/물류/원재료 리드타임
- 소비기한
- MOQ
- 생산 CAPA

### 출력
- 주차별 필요 생산량
- 주차별 발주량( MOQ 반영 )
- 생산/발주/납품 일정
- 캘린더 To-Do 이벤트
- 실제 사용량(수불 기반) 지표(예측 수요와 분리 표기)

---

## 5. 시스템 구성
- 프론트: Next.js
- 백엔드: FastAPI
- DB: PostgreSQL
- 배포: Vercel(프론트), Railway/Supabase/Render(백엔드)

구조:
사용자 -> Next.js UI -> FastAPI API -> PostgreSQL

---

## 6. 사용자 권한
| 권한 | 기능 |
| --- | --- |
| 관리자 | 전체 수정/설정 |
| SCM 담당자 | 생산/발주/캘린더 수정 |
| 영업 | 예상 판매 입력 |
| 조회 전용 | 조회만 가능 |

---

## 7. 운영 방식
- 판매 데이터: 매일 배치/동기화
- 재고 데이터: 실시간 또는 고빈도 동기화
- 생산 데이터: 생산 완료 시 즉시 반영

---

## 8. MVP 범위 (4~6주)
필수 기능:
- 제품 관리
- 재고 관리
- 예상 판매 입력
- 52주 계획 생성
- SCM 캘린더
- To-Do 생성

---

## 9. 현재 기획에서 반드시 포함할 요소
1) 안전재고(Safety Stock)
- 기준: 평균수요 + 리드타임/수요 변동성 반영

2) MOQ(최소 발주량)
- 계산 발주량이 MOQ 미만이면 MOQ로 올림

3) 생산 CAPA
- 일/주 생산 가능량 제약 반영

4) Lot 관리
- 식품/소비기한 제품 필수

5) 창고별 재고
- 공장/물류센터/매장 단위 재고 가시화

---

## 10. 확장 기능
- AI 수요예측(시즌성/이벤트 반영)
- 자동 발주(조건 충족 시 발주서 자동 생성)
- 공급사 포털(발주/납품 일정 공유)
- What-if 시뮬레이션(판매 +20% 등 시나리오)

---

## 11. 기대 효과
| 효과 | 설명 |
| --- | --- |
| 재고 최적화 | 과잉/부족 동시 완화 |
| 업무 자동화 | 발주/생산 일정 자동 생성 |
| 리드타임 관리 | 지연 위험 조기 대응 |
| 소비기한 관리 | 폐기율 감소 |
