# SCM 수불·재고 연동 — 요구사항 (마일스톤 기준)

본 문서는 `기획서_TEMPLATE.md` §2.5~§2.7, §3.2.1, §4 및 `5월 수불.xlsx`를 기준으로 **무엇을 만들 것인지**만 적는다. 구현 순서는 `plan.md`의 마일스톤을 따른다.

## 배경

- 엑셀에는 **재고DB**(WMS형 스냅샷), **출고량**(품목×일자 피벗), **완제품/원재료** 분석 시트가 공존한다.
- 웹은 동일한 **데이터 의미**를 갖되, 단계적으로 스코프를 넓힌다.

## 기능 요구사항 (우선순위는 마일스톤 순)

### R1 — 원장 데이터 계약

- 입고/출고 행에 **기준일(as_of_date)** 을 둔다. (월·일자 단위 반영)
- **lot_no**, **expiry_date**(소비기한), **unit**, **source_ref**(엑셀/문서 참조)를 원장에 보관할 수 있어야 한다.
- 거점은 기존 `warehouse`와 매핑 가능해야 한다.

### R2 — 재고 누적

- 원장(IN/OUT)에 따라 재고가 **일관되게** 갱신된다. (정책: 기준일별 스냅샷 또는 누적 규칙은 M3에서 확정·구현)
- 음수 재고는 거부하거나 정책에 따라 명시적으로 처리한다.

### R3 — 엑셀 연동

- **템플릿 시트**: 품목·구분·수량·거점·로트/소비기한 등 (현재 `/ledger` 업로드의 상위 호환)
- **출고량 시트**: 일자별 컬럼 피벗 형식을 파싱해 OUT 행으로 변환할 수 있어야 한다.
- **재고DB 시트**: 물류센터·품목·수량(가용 등)을 반영할 수 있어야 한다. (스냅샷 덮어쓰기 vs 트랜잭션 변환 중 하나는 M5에서 확정)

### R4 — Reconciliation

- **기초 + Σ입고 − Σ출고** 와 기말 재고를 비교할 수 있는 화면 또는 리포트가 있어야 한다. (범위: 월·거점·품목 등)

### R5 — FIFO·실사용량

- 출고(OUT)를 입력으로 **로트 단위 소진**을 계산하고, 결과를 **실제 사용량(실적 수요)** 으로 노출한다.
- 52주/대시보드 엔진과의 연동 시 **예측 수요와 구분**해 표시한다.

### R6 — 소비기한·위험 (기획서 §2.5·2.7)

- 임박·폐기·부족 등 알림은 R1~R5 이후 스코프로 둔다.

## 비기능

- 기존 API·프론트 패턴(`stock_transaction`, `inventory`, `item`, `warehouse`)을 최대한 존중한다.
- 변경 시 `backend/tests` 및 프론트 `lint`/`build`로 회귀를 막는다.

## 단일 기준 파일

- 엑셀: `5월 수불.xlsx`
- 기획: `기획서_TEMPLATE.md`
- **M1 매핑·정책(확정본)**: `docs/수불_매핑.md`

## 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-03-28 | 초안 작성 (마일스톤 우선 스코프) |
| 2026-03-28 | M1: `docs/수불_매핑.md` 연계 |
